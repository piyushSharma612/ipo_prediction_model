import React from "react";
import ReactDOM from "react-dom/client";
import SentimentTerminal from "./ipo_sentiment_terminal.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SentimentTerminal />
  </React.StrictMode>
);
